{
  "name": "HelloWorld",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node http://127.0.0.1:8020 HelloWorld",
    "create-local": "graph create --node http://localhost:8020/ HelloWorld",
    "remove-local": "graph remove --node http://localhost:8020/ HelloWorld",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 HelloWorld",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.1",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
